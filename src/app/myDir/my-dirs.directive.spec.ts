import { MyDirsDirective } from './my-dirs.directive';

describe('MyDirsDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDirsDirective();
    expect(directive).toBeTruthy();
  });
});
